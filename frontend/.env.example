# ============================================================
# frontend/.env.example — Variáveis de ambiente do frontend React
# Copie para frontend/.env e ajuste conforme necessário
# Variáveis Vite DEVEM começar com VITE_ para ficarem acessíveis
# ============================================================

# URL base da FastAPI — altere para o endereço de produção no deploy
VITE_API_URL=http://localhost:8000